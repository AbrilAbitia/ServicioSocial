extends layout

block scripts
  script(src='/js/sender.js')

block sidebar
  include sidebar.pug

block content
  div.main.container
    div.row
      div.col-md-6.col-md-offset-3
        h1.display-4.m-b-2 #{title}
        form#usuarioForm()
          div.form-group
            label(for='usuario') Usuario:
            input#user.form-control(type='text', name='user', value=usuario.user, disabled=isUsuarioDisabled)
          div.form-group
            label(for='contrasena') Contraseña:
            input#password.form-control(type='text', name='password', value=usuario.password, disabled=areFieldsDisabled)
          if areFieldsDisabled
          else if isUsuarioDisabled 
            button.btn.btn-primary(type='button', onClick='putUsuario()') Editar
          else 
            button.btn.btn-primary(type='button', onClick='putUsuario()') Registrar